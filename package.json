{
  "name": "workspace",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules packages/*/node_modules dist",
    "build": "bun --filter site run build",
    "dev": "bun --filter site run dev",
    "serve": "bun --filter site run serve"
  },
  "devDependencies": {
    "prettier": "^2.8.4"
  }
}
