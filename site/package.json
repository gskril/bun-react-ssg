{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun ../bun-react-ssg/bin/cli.ts build --pages src/pages --dist dist",
    "build": "bun ../bun-react-ssg/bin/cli.ts build --pages src/pages --dist dist",
    "serve": "bun ../bun-react-ssg/bin/cli.ts serve --dist dist --port 3000"
  },
  "dependencies": {
    "bun-react-ssg": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.1"
  }
}
